# Agents — Projects Root

This is the workspace root. New work starts here.

## Routing Map

Add one line per project so agents route correctly:

- `active/<SLUG>/` — <PROJECT_NAME>: <OUTCOME>

## Rules

- To start a project, run the `project-new` skill.
- Codex executes. Claude grills only at the planning gate (`plan-review`), once per plan.
- Keep `PROJECTS.md` current. Keep root `MEMORY.md` for cross-project durable context only.
