# Agents — <PROJECT_NAME> (basic)

Local experiment. No GitHub/Linear/plan-gate required.

- Outcome: <OUTCOME>
- Verify: `<VERIFY_CMD>`
- Keep `MEMORY.md` for anything worth remembering across sessions.

Promote to a managed project (run the `project-new` skill) if this outgrows an experiment.
